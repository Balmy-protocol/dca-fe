export enum IntervalSetActions {
  balance = 1000 * 60 * 10,
  selectedTokenBalance = 1000 * 30,
  // price = 1000 * 60 * 20,
  // transactions = 1000,
  allowance = 30000,
  tokens = 10000,
}

export enum TimeoutPromises {
  COMMON = '30s',
}
