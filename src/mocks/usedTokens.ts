import { UNI } from './tokens';

const mockedUsedTokens = {
  data: {
    tokens: [
      {
        tokenInfo: {
          address: UNI(1).address,
        },
      },
    ],
  },
};

export default mockedUsedTokens;
