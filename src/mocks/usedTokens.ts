import { UNI, YFI } from './tokens';

const mockedUsedTokens = {
  data: {
    tokens: [
      {
        tokenInfo: {
          address: UNI.address,
        },
      },
      {
        tokenInfo: {
          address: YFI.address,
        },
      },
    ],
  },
};

export default mockedUsedTokens;
